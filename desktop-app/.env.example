# Backend API URL. For desktop app (cloud-only/client mode), set to your production API:
# VITE_BACKEND_URL=https://xpress-scan.onrender.com
VITE_BACKEND_URL=http://localhost:8000

# Google OAuth for desktop "Continue with Google" (system browser flow).
# Use the same OAuth client as your web app (Firebase Console → Project Settings → Your apps).
# Add this redirect URI in Google Cloud Console → Credentials → Your OAuth client → Authorized redirect URIs:
#   http://localhost:8080/
# VITE_GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Supabase Configuration (optional, for cloud sync)
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
