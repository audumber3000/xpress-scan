services:
  - type: web
    name: xpress-scan-backend-docker
    env: docker
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    # Add any other environment variables your app needs
    # - key: DATABASE_URL
    #   value: your_database_url
    # - key: SUPABASE_URL
    #   value: your_supabase_url 